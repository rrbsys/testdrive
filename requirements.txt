# Core framework requirement — this is the ONLY thing needed for
# -L, -I, -M (and -M --json) to work. Verified: with only Pillow
# installed (torch/transformers/huggingface_hub/numpy/etc all absent),
# every one of those commands runs cleanly and reports per-plugin
# missing dependencies rather than crashing.
Pillow>=10.0

# Per-plugin backend dependencies — install only what you actually plan
# to run. Prefer `pip install .[<plugin>]` (or `.[all]` for every active
# plugin) over hand-copying these; see pyproject.toml's
# [project.optional-dependencies] for the authoritative, per-plugin list.
#
# torch>=2.2
#
# transformers is pinned EXACTLY to 4.50.3, not just a lower bound:
# every version after it we've tried introduces a regression that pulls
# in a TensorFlow dependency chain, which then breaks every plugin here.
# Revisit this pin once that's fixed upstream.
# transformers==4.50.3
#
# owlv2 additionally needs:
# scipy
#
# florence2 additionally needs:
# einops
# timm
#
# samgd additionally needs:
# numpy
# segment-anything     # unofficial third-party PyPI mirror of Meta's SAM;
#                       # see the comment in pyproject.toml for the
#                       # authoritative git+https alternative
